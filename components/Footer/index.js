import styles from './Footer.module.css';

function Footer(props) {
    return (
        <div>
            <p className={styles.title}>Made with Love @Pisangan</p>
        </div>
    );
}

export default Footer;